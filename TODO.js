// TODO(riley): Use this.
// const LOG_LEVEL = 2; // ["Errors", "Warnings", "Debug"]
// TODO(riley): Observe a "leader" property and update status onChange.
// bot.post('account/update_profile', {
//   description: 'I send math quizzes into the ether. Tweet at me with an ' +
//       `answer to get points. The current high score is held by @${leader}.`
// });

// TODO(riley): Add consolation prize winners.
// status: `${rand() ? 'So close' : 'Good try'} @${user.screenName}, ` +
//   `But ${winner} beat you to it.`;

// TODO(riley): Hello world!
// bot.post('statuses/update', {
//   status: 'Look at the world with the child\'s eye - it is very beautiful.'
// });
